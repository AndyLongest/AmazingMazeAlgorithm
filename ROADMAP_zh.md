# 迷宫算法学习路线

[English](ROADMAP.md) | 中文

## 学习目标

通过同一套网格数据结构，连续实现多个经典迷宫生成算法，理解：

- 每个算法的核心思想
- 时间/空间复杂度和实现代价
- 生成迷宫风格差异（走廊长度、分支密度、均匀性）

## 阶段安排

### Step 1: Recursive Backtracker

- 核心：深度优先搜索 + 回溯（栈）
- 特征：长走廊明显、方向感强，局部“先钻深再回退”
- 性能：时间 O(W×H)，空间最坏 O(W×H)
- 易错点：未访问邻居筛选与回溯弹栈条件

生成流程（按步骤）

1. 随机选择一个起点格子，标记为已访问，并压入栈。
2. 查看栈顶格子的四邻域，筛出未访问邻居。
3. 若存在未访问邻居：随机选一个并打通墙，标记后压栈。
4. 若不存在未访问邻居：弹出栈顶（回溯）。
5. 重复直到栈为空。

### Step 2: Randomized Prim

- 核心：从已连通区域边界随机扩张
- 特征：分支更密、短通道更多，形态偏“灌木状”
- 性能：时间近似 O(W×H)，frontier 管理影响常数项
- 易错点：frontier 去重与访问状态判断

生成流程（按步骤）

1. 随机选起点并标记已访问。
2. 将起点到未访问邻居的边加入 frontier。
3. 随机取一条 frontier 边：
   - 目标已访问则丢弃；
   - 目标未访问则打通并标记访问。
4. 把新格子可扩张边加入 frontier。
5. frontier 为空时结束。

### Step 3: Randomized Kruskal

- 核心：把每格视为集合，随机拆墙并并查集合并
- 特征：全局随机、整体较均匀，方向偏置小
- 性能：时间约 O(E α(V))，网格中 E≈2WH-W-H
- 易错点：候选边重复与 DSU 实现错误

生成流程（按步骤）

1. 初始化并查集：每个格子是独立集合。
2. 枚举候选墙（常用东/南）并随机打乱。
3. 依次处理每面墙：
   - 两侧集合不同则拆墙并 union；
   - 相同则跳过避免成环。
4. 全部处理完成即得到迷宫。

### Step 4: Wilson

- 核心：Loop-Erased Random Walk（消环随机游走）
- 特征：生成均匀生成树（UST），统计公平性强
- 性能：通常快于 Aldous-Broder，但实现更复杂
- 易错点：路径消环顺序错误

生成流程（按步骤）

1. 随机选一个格子作为初始树。
2. 从任一未入树格子开始随机游走并记录路径。
3. 若路径自交，擦除环段。
4. 路径撞到已有树后停止游走。
5. 将无环路径整段打通并并入树。
6. 重复直到所有格子并入树。

### Step 5: Aldous-Broder

- 核心：随机游走，首次访问新格子时才连边
- 特征：同样得到 UST，理论优雅但后期空转明显
- 性能：单步 O(1) 但总步数高，覆盖时间成本大
- 易错点：重复访问时误打墙

生成流程（按步骤）

1. 随机选起点 `current` 并标记访问。
2. 每步随机走到邻居 `next`。
3. 若 `next` 首次访问，则打通 `current -> next` 并标记。
4. 无论是否首次访问，都令 `current = next`。
5. 所有格子访问完毕后结束。

### Step 6: Sidewinder / Binary Tree / Eller

- Sidewinder

  - 核心：按行维护 run，向东延伸并随机向北泄压
  - 特征：水平偏置明显、长横向走廊常见
  - 重点：每行完整扫描；每个 run 结束时仅一次向北连通

  生成流程（按步骤）

  1. 逐行扫描，维护当前 run。
  2. 当前格加入 run，并决定向东延伸或关闭 run。
  3. 向东延伸时打通东墙。
  4. 关闭 run 时在 run 内随机一格向北打通一次并清空。
  5. 行尾必须关闭 run。
- Binary Tree

  - 核心：每格仅在两个固定方向中选一个开墙
  - 特征：实现最简单，但方向偏置最强
  - 重点：适合作为低成本对照基线

  生成流程（按步骤）

  1. 为每格设定两个候选方向（如北/东）。
  2. 随机选一个在边界内可行的方向。
  3. 打通该方向墙并继续扫描。
  4. 全网格扫描结束即完成。
- Eller

  - 核心：逐行维护集合，行内合并 + 向下传递
  - 特征：流式生成友好，空间 O(W)
  - 重点：每集合至少一个向下连接，末行强制全并

  生成流程（按步骤）

  1. 为当前行未分配格子补集合号。
  2. 行内相邻不同集合可按概率横向合并。
  3. 对每个集合随机选择一个或多个格子向下打通（至少 1 个）。
  4. 向下打通位置继承集合到下一行，其余新建集合。
  5. 最后一行强制合并所有剩余不同集合。
